<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="32dp"
    android:gravity="center">

    <!-- Анимация успеха -->
    <ImageView
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@drawable/circle_play"
        android:layout_marginBottom="16dp" />

    <!-- Заголовок успеха -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Подарок отправлен! 🎉"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/green_500"
        android:gravity="center"
        android:layout_marginBottom="8dp" />

    <!-- Изображение отправленного подарка -->
    <ImageView
        android:id="@+id/sentGiftImage"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_marginBottom="16dp"
        android:scaleType="fitCenter"
        android:background="@drawable/bg" />

    <!-- Сообщение об отправке -->
    <TextView
        android:id="@+id/sentMessageText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Подарок успешно отправлен пользователю!"
        android:textSize="16sp"
        android:textColor="@color/black"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:layout_marginBottom="24dp" />

    <!-- Дополнительная информация -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp"
        android:layout_marginBottom="24dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="✓ Подарок доставлен"
            android:textSize="12sp"
            android:textColor="@color/green_500"
            android:drawablePadding="4dp"
           />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="✓ Получатель уведомлен"
            android:textSize="12sp"
            android:textColor="@color/green_500"
            android:drawablePadding="4dp"
            android:layout_marginTop="2dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="✓ Подарок отобразится в профиле"
            android:textSize="12sp"
            android:textColor="@color/green_500"
            android:drawablePadding="4dp"
            android:layout_marginTop="2dp" />

    </LinearLayout>

    <!-- Кнопка OK -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/okButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Отлично!"
        android:textSize="14sp"
        app:iconTint="@color/white"
        app:backgroundTint="@color/green_500"
        app:cornerRadius="20dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp" />

</LinearLayout>