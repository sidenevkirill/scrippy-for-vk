<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <!-- Контейнер для превью стикера -->
    <LinearLayout
        android:id="@+id/previewContainer"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginEnd="16dp"
        android:background="@drawable/bg"
        android:gravity="center"
        android:orientation="vertical">

        <!-- ImageView для обычных стикеров -->
        <ImageView
            android:id="@+id/imagePreview"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:scaleType="centerInside"
            android:visibility="visible"
            tools:src="@drawable/ic_sticker_placeholder" />

        <!-- TextView для эмодзи стикеров -->
        <TextView
            android:id="@+id/textPreview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="😊"
            android:textSize="20sp"
            android:visibility="gone"
            tools:visibility="visible" />

        <!-- Бейдж для отключенных пакетов -->
        <View
            android:id="@+id/disabledOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#80FFFFFF"
            android:visibility="gone"
            tools:visibility="visible" />

    </LinearLayout>

    <!-- Основная информация о пакете -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/textPackTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Название пакета"
            android:textSize="16sp"
            android:layout_marginTop="15dp"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:ellipsize="end"
            android:maxLines="1"
            tools:text="Базовые эмодзи" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:visibility="gone"
            android:layout_marginTop="4dp">

            <TextView
                android:id="@+id/textStickerCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:text="0 стикеров"
                android:textSize="14sp"
                android:textColor="@android:color/darker_gray"
                tools:text="50 стикеров" />

            <View
                android:layout_width="4dp"
                android:layout_height="4dp"
                android:layout_gravity="center_vertical"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:background="@android:color/darker_gray"
                android:visibility="gone" />

            <TextView
                android:id="@+id/textPackStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Отключен"
                android:textSize="12sp"
                android:textColor="@color/black"
                android:visibility="gone"
                tools:text="Отключен"
                tools:visibility="visible" />

        </LinearLayout>

        <!-- Дополнительная информация (цена, статус покупки) -->
        <TextView
            android:id="@+id/textPackInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Бесплатно"
            android:textSize="12sp"
            android:textColor="@android:color/darker_gray"
            android:layout_marginTop="2dp"
            android:visibility="gone"
            tools:text="Куплен • 100 руб" />

    </LinearLayout>

    <!-- Переключатель включения/отключения -->
    <androidx.appcompat.widget.SwitchCompat
        android:id="@+id/switchEnable"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="16dp"
        android:buttonTint="@color/black"
        android:clickable="true"
        android:focusable="true"
        tools:checked="true" />

</LinearLayout>