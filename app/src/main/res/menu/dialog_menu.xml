<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_search"
        android:icon="@drawable/ic_search"
        android:title="Поиск по сообщениям"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_clear"
        android:icon="@drawable/close"
        android:title="Очистить историю"
        app:showAsAction="never" />
    <item
        android:id="@+id/menu_info"
        android:icon="@drawable/information_outline"
        android:title="Информация о диалоге"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_manage_stickers"
        android:icon="@drawable/ic_info"
        android:visible="false"
        android:title="Управление стикерпаками"
        app:showAsAction="never" />

</menu>